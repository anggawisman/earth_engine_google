# Earth Engine credentials — copy to .env and fill in values
# Never commit the actual .env file or service account JSON

GEE_CLOUD_PROJECT=your-gcp-project-id
GEE_SERVICE_ACCOUNT_KEY_PATH=./credentials/service-account-key.json
